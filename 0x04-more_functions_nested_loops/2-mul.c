#include "holberton.h"
/**
* mul - name of the function for cheking digits
* @b: factor
* @a: factor
* Return: the product of the multiplication
*/
int mul(int a, int b)
{
int c;
	c = (a * b);
	return (c);
}
